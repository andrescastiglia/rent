# Loaded in the nginx http context. Exclude query strings and Referer because
# document capability URLs contain short-lived credentials.
log_format rent_no_args '$remote_addr - $remote_user [$time_local] '
                        '"$request_method $uri $server_protocol" '
                        '$status $body_bytes_sent "$http_user_agent"';
